sticky bit 1000번대 이지만 1777만 존재

&1 출력파일에 error 코드까지 담겠다 인데 &1 이 순서의미인가

systemvbanner system five banner

git fetch :remote에서 변경된 사항이 있으면 다른사람이 올렸는지 확인하기위해 먼저해줘야한다 그걸 확인하는게 fetch 이다.

프로그램 : 실행가능한 프로세스 아직 실행되지않은 상태
프로세스 : 현재 시스템에서 실행중인 프로그램
fork? :

PPID 부모 프로세스 id? 0 번은 없는것 
d가 붙은 프로세스 damon? 
초기화를 하기위한 스크립트 /sbin/init splash 이게 systemd 인게 있다.

리눅스 진영 크게 두가지 redhat systemd임 debian
리프레시 클럭? 
수세 리눅스 독일(특히 많음) 유럽

/sbin/init  unix에서 옴 

redhat에서 systemd 를 init 대체해야되겠다 해서 만듬.
POSIX 에선 systemd 를 따라라 라고 표준으로 지정함.

거대 벤더가 실제 사실적 표준을 무시해버리는걸 사실표준이라함

systemd 를 관리하는 명령어가
systemctl 이다 . 
근데 우분투에 있다.


systemctl restart ssh 가 표준 이다
service ssh restart도 가능하게 해놨다. 우분투 고집쓰 

데몬을 띄우는 예전 명령어 init 시대 명령어이다.
init 을쓰는거야 하는것처럼 껍데기 해놓고 사실은 아니다 .ㅋㅋㅋ

데몬은 프로세스 종류중 하나이다.
프로세스는 고유번호 가짐 PID

데몬이라는게 포트 고정되서 관리된다.

ip는 주소
tcp는 주소아님 protocol
직배 아파트앞 까지만 가는게 ip
사는 사람들에 대한 명시 port 

port는 네트워크 서비스에 종류를 구별하기위해서 사용한다. 
데몬은 특정서비스를 제공하는거라 etc/services 에 모여있음


BSD 가 뭐라고했떠라

가상 메모리 swap 이다.

세션이라 부른다 port를 ?

pgrep process grep 이다 .

kill -l
1, 2, 3,9 ,15, 20 은 무조건 외워야한다
전부다 종료시키는 방법이긴하다.

sigint interrupt 사용자가 종료 2번 
terminater 는 15번 

1 -SIGHup, logoff 끊김 당한것 logout과 다르다

20 - SIGTSTP , ^z 프로그램 끝났다가 재시작하는것

fork :부모프로세스가 자식프로세스를 실행하는것
19 - sigstop : ?

kill -9 3634 bash 쉘은 -9로 죽여야된다.

정렬방식 이 바뀐다는것이다.

jobs 백그라운드에서 돌고있는걸 확인하는 명령어 
포그라운드 상태에서 ^z 누르는거 멈추는것

man ls & -> &가 뭐지?

fg 포그라운드로 멈춘걸 다시 시작 하는건가? + 모양인게 마지막 작업한거
fg %2 두번째거

& 엔퍼센트 붙여주면 백그라운드에서 동작하게 할수있다.

bg 백그라운드에서 실행하는것

kill %1 작업넘버임 그걸로 죽일수있다.

hub 는 작업이 끊겨버리는걸 말한다.

nohub 아주 오래걸리는걸 맡기고 퇴근하면 nohup 하면 절대 안죽는다.  컴퓨터만 꺼지지않으면 절대안꺼진다.

PR Prade? prior process priety? 프라이어티 우선순위값 OS에서 관장한다.사용자의 조작이 불가능

NI nice 값 : user 관리 프라이어티 프로세스 먼저시작하냐 마냐 를 물어보는건데  -20 19 0은 default -20 에 가까울수록 가장 낮은 프라이어티 제일 우선순위 높음
19가 가장 낮은 우선순위 
0은 default

renice 나이스 바꿀수있다.

nice vs renice 

nice : 상대적으로 값 변경 가능 절대값도 가능 -n [n] 프로세스 값이 두개?

renice : 증감이 아닌 그냥 바꿔 준다 절대값으로 변경 -10  

우선순위 높이고 낮추는건 root만 가능하다 tstp

nice를 상대적으로 바꾸려면 -10을 nice --10 bash 이렇게 프로세스명과 -- 를 붙여서 -10임을 넘겨줘야된다 . 상대적으로 

윈도우 at만있다. crontab이 없다 .
crontab 이 여러번 실행 할수있다.
at [시간] 하면 at prompt가 뜨게 되고 빠져나올땐 ^d

at -l
atq 확인 남은 목록에 없으면 안뜸
지울때 지울수잇는 방법 두가지

모순 창모 방패 순
보안이 모순이다. 보안은 완벽 하지 않다.  보안이 어려워서 iphone 보안성이 되게 좋아서 쓴다. Android 보안성 취약하다. Android는 보안에 자신이있는것 ios 자체가 자신이없는거다.

보안하는 방식 . allow허락하다 나머지 다거부하는데 요거만 허락해줄게 deny 거부 다허락하는데 이거만 거부할게 애플은 allow 방식 앱단위로 간격이있다. 강력하게 봉쇄해논것이다. 음 보안은
이런식으로 해야되나 ?

/etc/at.allow 이거는 허용하는범위야 
/etc/at.deny 안들어있으면 다 deny 서비스다 다막혀있다. 

ufw allow ssh ssh 이것만 허락해줘.소프트웨어적인 방어벽 쓸정도 하드웨어 재밌을거같에
얇게 만드는게 코어를 늘려놓고 있다. 하드웨어 설계원칙이있다. 
전기는 물과같아 100만큼 힘을써서 보내야하니 전기 가 많이 소모된다는건 열이 발생 열이 많이 발생 팬 이커져야한다. 
panless 열이 발생하는것 열이 얇게 만든것 자체가 cpu 가 약해진것 1/4 크기 만해진것 적게 가게 파워가 약하다 . 
팬이있는게 쌘거라고 봐야하나 광고에 속아서  크고 세고 두꺼운것 얕고 가볍고 좋은노트북은 없다.

crontab이라는 명령어는 cron 관리

* 는 그범위 모두포함 

*/2 는 짝수만

*/30 30마다
0,30
*/5 5의 배수마다 

cron 은 db 작업 자들이 많이씀 새벽에 이뤄지게 한다.
컴퓨터는 알면편하고 모르면 고생한다. 

ZZ 저장하고 빠져나가기?

스토리지 매니지먼트 가 중요하다. 

# apt-cache stats 

특정한 걸 확인하고싶을때 show [패키지] 사용하기

# apt-get check 의존성 관계 무시하고 삭제된게있는지 확인하는거
# apt-get autoclean

xterm 원격에서 설치하면 안보임 

# apt -y remove xterm 지우기

패키지저장소에서 패키지 정보만 가져오는것 apt-get update
마이게 패키지 관리다

수동으로 설치해줘야되는경우 원격 으로 다운로드 하는게 아니라 
/tmp stickybit 라 reboot하면 사라진다

dpkg -l 확인

dpkg 는 데비안 패키지 관리 시스템의 기초가 되는 소프트웨어로서, .deb 패키지의 설치, 삭제, 정보 제공을 위해 사용되는 명령어
이걸로 봐서 ii 가 앞에 되어있는건 이미 설치된것 rc 는 설치안된것

wget 사용자가 직접 주소검색하는 경우 인데 정식 배포판에 포함안된경우 이렇게한다.
# dpkg -i xterm_330-1ubuntu2_amd64.deb
deb 파일 로 install시 이렇게 

설치시 의존성 순서대로 설치하고
지울시는 설치한 반대로 지운다 dpkg -r [package name]
설정파일까지 다 지워주는거 dpkg -P [package name]

파일한두개만 지워진경우
이것도 압축 파일이니까

dpkg -x libtinfo5_6.3-2_amd64.deb libtinfo5 압축파일 풀면 폴더로 저장되고 거기서 파일 몇개 꺼내서 제대로 동작하게 할수있다.
