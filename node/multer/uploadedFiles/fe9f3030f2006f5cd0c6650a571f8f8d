snap list 깔린 snap 소프트웨어?

tar tvf [tar파일명] 묶음을 풀지않고 확인만도 할수있다. 랜덤 액시스 디스크 파일에 형식? 을 순서대로 묶어주기만하는것이다.

기존에 있던거에 추가는 rvf
업데이트는 수정 본은 uvf 

allnew 를 묶을거면 allnew 안에서 묶으면 안된다 recursive 하게 계속 묶이게 된다.
그 상위 폴더에서 묶어야된다. 
/work/allnew

source install에 장점 최적화
바이너리 패키지를 설치하면 쉽고편하긴한데 / 단점 오류 발생 가능성이 아주 높다. 내시스템에 최적화가 안되어있다.

configure 전체시스템 을 조사한다. 어떤 어떤 라이브러리가 설치되어있고, 어디에 위치해 있는지 조사한다.

prefix install 이후 설치될 디렉토리 지정한다.

make 지정 하는것?

apr-iconv 는 apr에 의존성이 잇어서 --with-apr 로 설치된 위치를 줘서 설치되어있다고 알려줘야한다 .아니면 못찾아서 에러남 

configure 에 결과로 makefile이 만들어진다. 
automake makefile 

make 라는 동작은 makefile이 있어야 가능하다.
p - primary 
e - extends 인데 이차이를 안다고믿고 찾아보자 
mkfs 만들어진 상태에선 /dev/disk/by-uuid/ 가 만들어진다 이걸로확인한다는건가 내용은 못보네 만들어진것만있네 

boot block 망가지면 지금은 복구불가 뭐가안되면 이라고했지

meta 데이터 : 전혀 가공을 하지 않은 데이터 , 뭉치인뜻

메타 버스 : 현실에 대한것들을 미러링해서 만든세계 어딘가에 잇을법한 세계를 만든것

서로 흩어져있는 물리디스크를 합치는게 의미가 있는거다. 

LVM 을 위해 만들어논 실제 볼륨 PV

LVM 은 쪼개고 합치는거 다가능 legacy 는 쪼개기만가능 합치기 불가능 
원래디스크 관리방식은 레이드? 방식으로 관리하는게 좋다 . 여러개를 묶어서 병렬적으로 관리하는 방법

1 미러블? disk 두개를 사서 하나의 있는 데이터를 하나가 백업 하는게 좋다 . 그렇게할수없으니 소프트웨어 적인 manage를 할거다.

mkfs ext2 부터만듬 영어 e 가 추가되면 jurnaling? 된다 ... 

다른 파티션에 붙었다가 다시 할당하는거라 묻는거다? signature

Type 이 Linux 로 되어있다 . 이상태는 LVM에 쓸수가없다. 
t 모드로 변환

vgchange -a y vg vg 를 activation 한다는 뜻이다.

Alloc Pe / Size : 0 / 0 하나도 할당 안되어 있다는 뜻이다.

lvcreate -l 765 vg -n lvm1 765개의 lv를 만들었다는 뜻이다.

이걸 disk 파티션으로 만들어야한다.
mkfs.ext4 /dev/vg/lvm1 이명령어로 가능? 이게 파티션으로 만드는거야?

새로 생성된 file system은 그 폴더에 lost+found 가 보여야한다. 있어야한다.

dd data dump 뜨는 명령어 /dev/zero  null 파일 null 로 데이터 채움 장치 파일 swap 같은거 만들때 많이 쓴다? dump 파일 ? 

/dev/vg/lvm1다시 키면 - >/dev/mapper/vg-lvm1

lvm 해제하기 
거꾸로해야되고
mount 된상태로하면 filesystem에서 사용하고 있다고 못지움 umount 해줘야한다.

wipe 라고 뜨는이유 remove가 아니라 권리자의 사용권한이 없어진것뿐이라는 의미이다.

3개 만 vg 에 포함시켜서 언벨런스한 lvm을만들것이다.
데이터 양쪽에 넣고 두번째에다가 새로운걸 하나를 추가해서 리사이징하는 과정까지 쭉진행한다.

 Free  PE / Size       0 / 0 vgdisplay 에서 아직 할당안된 PE 를 볼수있다. 

4개를 일단 3개만 포함 시켰쥬 
하나의 255가 만들어지네 1GiB 공간 을 510 을 똑같이 맞춰주게 합쳐줄것이다 . 데이터 가 있으면 문제가 되니 데이터
데이터 volume 이 늘릴때 문제가안되지만 2TB -> 1TB 로 줄일때 문제가된다 데이터가 퍼져잇을거라서 

공간이 1TB 있으면 줄일수있지만
중요한 파일 있으면 깨질 확률은 60% 이다. 

 vgextend vg group에 추가하기
lvextend [-L(절대적추가) ,-l (상대적 추가) ex)+255

데이터 붙어있는상태에선 resizing하면 안되겠죠 . umount 하고 해줘야한다.

lv 관리하는것과 실제 disk 관리하는것은 다르다. 

e2fsck -f /dev/vg/lvm2 파일 한번 체크해주는것이다.

-f 는 강제적인 느낌이있다.

식사전에 할 숙제 ----------------------------------------------------------------


lvm1 - 510 에다가 푼거 255 를 여기다 붙여
lvm2 - 510 을 다시 255 로 만들고







=====================================================

레이드 하면 빡이 칠거다. 

불량난 하드 
파티션 개념이 아니고 물리적인 디스크 가 같아야한다. 레이드 하려면

하드 8G
하드 10G 두개를 미러링할수있으면 좋은데 안됨 무조건 같은 용량에 디스크여야한다.
단위 모듈이 2G 되게나누면 그때는 2G 단위로 레이드 할수있다 .

파티션으로 나눠버려가지고 파티션가지고 레이드를 할수있다?

불량난 넘버는 체크 가능 
CD 에서 세로로 나눈것 sector

부채꼴 모양이 생긴다 나누면 말이다. 

똑같은 용량에 볼륨들을 같이 쓰는거? 뭐지 

IO가 분할되서 속도가 늘어난다. 기하 급수적이다.
RAID0 4개 이상은 되어야한다.
데이터 누락할 가능성이 높다. 그래서 속도만 중요한 데이터 별로 중요하지 않은경우 0번

1번 

3/4 parity 체크 짝수 패리티를 한다 . 디스크 4개 하나 가 패리티 
1 1 0 순서 적으로 짝수로맞춰주는것? 0
0 1 0 	1로 함 짝수로 맞춰줌  

디텍션만 됨 아 여기 문제가 있구나만 알아 복구는 못해 
홀수 짝수 둘다 있다 . 패리티는 맞나?

단점 : 3개의 disk 패리티를 1개의 디스크에 넣으니 병목현상 생길수있다.
1개 만 복구 가능한게 홀수를 같이쓴다는건가? 

RAID 5 - Parity Across Disk : 전체 디스크에 나눠서 패리티를 저장하는것

RAID 6  디텍션 밖에 
더블 패리티? 두개를 두개에 디스크가 고장나도 복구할수있다.
1+ 0 , 0 + 1
안정성은 비슷해
속도는 0 + 1 가 더 빠르다 .

mdadm : meta device admin 물리적으로 떨어져잇는걸 모아서 가상에 디바이스? 디스크? 를만드는것
lvm 은 미러링이 안된다. 이건 된다 근데 조건이 까다롭다 같은 크기여야된다.


한번도 작업안했던거에 하면 creating array 할건지 안물어본다 그냥된다

보는 방법 두가지 다 .

디스크니까 UUID 보여야한다 UUID 가 뭐라고했지.

장치명만 가지고 파티션 개념없이 mdadm 으로 장치만 가지고 disk 만가지고 disk를만든것? 아닌데 장치 만가지고 disk를만든것?
조건이 사전에 같은 용량이란게 기준이 되어있어 파티션을 굳이 나눌 필요가없는것이다.
 
umount: /md0: target is busy. 거기안에 현재 워킹 디렉토리로 있는상태에서도 이렇게나옴

방금한건 그냥 선형 레이드 였다 . mdadm 

스트라이핑 볼륨

그만큼 소비되는게 	별로없다. 그만큼 정보가 없어

meta 가상 ,뭉치

overlay 가 뭐지 

왜 안하냐면 원론적인 설명을 하면 머리아픙께 이해하면서 이해하는 정도로 해야된다. RAID를 논리적으로 구현해보는것이다. 물리적으로 구현된것인가

lvm 장점 리사이징이 가능하다. 이론적으론 된다. 볼륨이 유연하다. flexibility 쓰다가 볼륨이 부족하면 언제든지 추가 할수있다. 최대단점은 lvm 은 미러링안됨

물리적인게 이해가안가
RAID 는 grade 가 있다. RAID 최대 단점 랙을 늘려야된다? 서버가 랙이 붙어있는 형태 레이드만 따로 나온다 걔를 늘려야된다. 
5번이 가성비     

1046528 blocks super 1.2 [2/2] [UU] UU uplink? 동작중 이라는의미

mirror volume 이라 하나만 씀

dd if inputfile of output file bs block size = 1M 1023 count=700 700개
 2066  ll /dev/disk/by-uuid/
 2068  blkid | grep md
둘다 uuid를 보는 방법 

UUID="41e9ae54-14e7-44e4-b68d-1ceeb954f24d"
UUID="987f177d-252e-4cf7-8707-e5e42b0961f5"
UUID 로 동작한다. 장치명으로 넣으면 안된다 리부팅후에 사라져버리기 때문에 UUID로 넣어야한다. RAID는?''

오라클  DB 실력을 보는것

노드 html css node.js 까지만 crontab shell script 내가 만든다고 
파일 데이터 원천 데이터(직접 손입력)


ux 까지 해야된다.
사용자 편의 위주 로 CRUD 하기 편하게 만들어야한다 . 검색 like 검색할수잇는지