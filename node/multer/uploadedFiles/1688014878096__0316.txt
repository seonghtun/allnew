raid 5 
parity 저장하는걸ㄹ로 3개 저장소를 쓸때 1개를 그렇게쓰기때문에 손해가있고 4개 5개 정도부터 이득이있다. 빠르고 가성비가좋다.
blkid = blockid

4 kb 가 하나의  mib 하나 아님? 
내가 찾는게 ram 이 아닌 cache 에있으면 hit ? 라고한다.
히트가 많으면 좋은거다.

캐쉬서버라는게 있다 업데이트 된 정보를 미국 서버에 가서 가져오는것보다 미리 국내에 미러링해두는것 캐쉬서버라고한다.

엔타이얼 디스크 전체를 사용한거기 때문에 파티션을 나눌필요가없다 이게 무슨뜻인가

zpool attch [pool name] [어디에다가] [뭘붙일건지]: zpool attch allnew sdb sdc array 가 생성됬나? 아닌데 이건
디스크 아래 물리적인 이름 dev 하위에 있기때문이다.

grub 부트로더

인터렉티브한 방식 inetd 느리다.

종료프로세스 에서 superblock 과 kernel 같은 RAM에서 DISK 로 옮겨줘야하는 정보가있는데 그걸 해주는 명령어가 sync 라는 명령어이다.

3가지 롤
프론트
백
하이브리드 : PM 이다. 

port 는 /etc/services 에서 볼수있다.
서비스 요청이 서버로 들어오면 설명 애니메이션에선 택배에 박스에 서비스 번호 가 적혀있다. 로봇이 같은게 이걸 들어서 21번이면 ftp 택배 분류 하듯이 분류하는 애니메이션

목적지 정보를담은게 패킷헤더 

슈퍼 데몬 과 stand-alone 이라는게 중요하네
gdm3 는 윈도우 매니저 

─wpa_supplicant wifi ?

53 번포트 domain name services?

smb 삼바 서비스는 윈도우즈에서 주변 서비스와 어쩌구 

ntpd : network type protocol daemon 

LILO : linux loader 

rdate -s time.hora.net 

ro : read only 
rw: read write 수정이가능한것

rescue 싱글 모드인가?

mount -o remount , rw / 마운트 -o 는 옵션을 remount 해라 rw로 / 리눅스 루트 파일시스템에 적용한다.
cdrom 으로 어떻게 복구할까 ? 다틀린 이야기 인터넷

암호복구? 인가이게
라이브 cdrom 부팅? try ubuntu 잠시 시스템 쓸수있는 화면으로간다
그러면 terminal 이있고

usb 리눅스 부팅 용 작은것 

프로그램 작성할때는 한줄을 밑에 남겨주는게 좋다
컴파일러가 프로그램 종료를 알수있는 약속 프로그래머 간에 말이다. 맨밑에 공백줄하나.

실행파일이 완전 2진수 기계어 파일.
object 파일을 RAM 에 올린다. 라이브러리를 사용할수있는 파일들을 불러와서 link 해준다
컴파일은 컴파일러 어셈블은 어셈블러 링크는 링커가 한다.
실행파일 되면 
loader 라는 애가 시스템에 모든 파일을 모아서 실행파일 로 만든다?

main에 아웃풋 파일? 그게 return 값 제대로 실행했어 라는 의미 정상적인 종료는 0 이 return 된다. 아니면 0 이외에 값이 된다.

#include <stdio.h>

int main(void) {
  printf("Hello, World~!!\n");

  return 0;
}

hello -o hello.c 바꿔도 된다고햇다고 이렇게해버리면 소스파일 사라짐

include 이 헤더 파일을 내 소스파일에 포함시키겠다.
< > 로 묶여있는거 시스템 헤더?라는것이다. gcc에 포함되어있는것
표준 헤더라 파일명만 써도 괜찮다.

링크 과정에서 재배치를한다.
relocated 

시스템은 분리한다 .printf 함수를 분리 -> parsing 이라말한다.
printf 가 나오면 문자만 인식 A 로 바꿔놔 

다 쭉읽어 실제적으로 실행할때는 전처리끝난거 함수 
linker 단계에서 실제 라이브러리랑 A 랑 바꿔치기한다.

오브젝트 파일단계도 해석만 해놓은 상태고 코드가 없다. 

링크 단계에 실제 그 라이브러리 코드를 바꿔치기해준다? 재배치 라는 의미가 이것?

그전에 실행못됨 ? 

RAM  에올리는건 loader 가 올린다 .흐므흐므

boot-iso 라이브 코더? 부터? 

atom 쓸때 work 를 ㅎ시작 디렉토리로 만들었더니 계속 allnew가 중복 생성되었네

gcc 는 /usr/include 에서 시스템 라이브러리를 찾는다
근데 같은 경로에 있지 않다 이럴때 저 header file 이있는 디렉토리 를 알려주는게
- I 이다.

라이브러리 경로 -L 
진짜 라이브러리 쓰는 라이브러리 이름은 -l 이다. 

소스는 안주고 싶고 자랑은하고싶다. 라이브러리 만들어서 

call by address 주소에의해서
call by value 값이 그냥 전달 x 라고만 주면 -> &x[0] 엔퍼센트에 [0]이 주소의 번지수 이게 생략된것이다.

값을 꺼낼때는 전체주소에 대표주소를 전해야된다.

왜이런 복잡한 데이터 타입들을 만들어놨나  5를 저장하면 101로 저장되고 앞은 공백 거꾸로 시스템도 있다 뒤부터 0으로 

L . E PC 기준 거꾸로 뒤집어야되서 overhead가 생긴다?
B. E  네트워크 , 서버 기준

데이터 데이터 데이터 

오브젝트파일은 따로 떨어져있어 쓰기 힘드니까 라이브러리로 만든다.
archieve? 묶어놓는것을 의미 저장소인줄
rs 로 일종에 네이밍 하는걸  말한다. ar r 만하면 오브젝트가 합쳐져서 무언가로 있는것이다. 
openAPI 로 줘버리면 소스다 볼수있다.
library로 주면 볼수없다.
아이게 다른거구나.

지금 centos ios는 부팅까지만 되는것 패키지 설치는 받아서 따로해야된다?!
그러면 아무것도없는건가

SMTP 메시지보낼떄 IMAP, POP3 : 메시지 받을때 

ALSA, OSS 사운드 카드  XSANE 스캐너 

physical volume : pv pe : 일반적인block 크기 
suse : zypper  redhat: yum ,rpm  
emacs 제임스 고슬링 

프로세스를 생성방식은 : fork
inetd standalone 데몬을 띄우는 방식

centos
mirror.centos.org/centos/8/BaseOS/x86_64/os/

boot 로 해서 설치원천으로 네트워크 상으로 다운받고있어서 느린거네 
네트워크 

centos는 부팅이 느리고 부팅후 빠르고
ubuntu는 부팅이 빠르다.