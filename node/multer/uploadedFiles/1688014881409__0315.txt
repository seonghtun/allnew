ICT 멘토링 사이트에서 참여하기 > 프로젝트 참여하기
대학생들이 프로젝트 올림 많이 올림 프로젝트 아이디어 컨셉 보면서 아이디어 보기 


멘토 팀이 가져온 프로젝트에 살붙여라 라라라랏 
멘토님이 만든 프로젝트지만 왜해야되는지 설명하는건 나다. 우리다.

건의사항이 있다. 좋은 건의사항이 있다.
알바도 알선해주남..

과정 중간중간 기업 탐방
장비 신청해서 쓸수도있구만.

fsk 
패키지 개발자 pip 개발자 npm 개발자
os 개발자 하고싶음

-f force 에 약자 강제적으로 

-L  255 로 절대적으로 255개로 만들수있다.
-l  -255 상대적인것

LVM 클라스어빌리티? 미러링이 안됨

RAID 같은 사이즈에 용량이 준비 된상태에선 가능하다.

최소 2개인데 거의 4배수가 많다 . 두개인것은 거의없다.

전체를 같다 보기때문에 파티션이 필요없다. == 파티션 나누지 않아야한다? 맞나
linear 는 선형적으로 기억하는 level 이다.

100개 을 기록 해야된다 . 첫번쨰 데이터를 디스크 당 50개 50개 나눠서 기록하면 스트라이핑
RAID 양쪽 디스크를 같이 써버린다.

linear는 100개 기록하다 부족하면 나머지 디스크에 기록 그래서 느리다 . 한번 다른걸 같이 쓰지 않으니까 

volume 이 올리는 순서에 따라 다르다.
사용을 하다가 잘못되는 경우도 생기기도한다 ?
lvm over raid md127이 귀신같이살아난다?

그래서 md127 로 왜 만들어지는거지? 

RAID 에 참여 하는 디스크가 비싸다 어덯게 살수있을깡

1인 sdc를 망가트리기

sdd(S)[0] 이면 spare라는 의미 이다.
recovery 회복

hot removed ? 동작중을 의미하나 hot 이라는게  서버 RAID 동작중에 디스크를 빼도 꽂아도 에러가안나 가격대가 있다.

아까 (f) 에 약자는 fail 이 아닌 fault 에 약자다
소프트웨어 raid 지만 실제 raid 랑 똑같이 동작하는것이다.

미러링 되고있는건 그냥못뺀다
고장 처리되야됨 --fail 옵션

그러면 순서가 [1]로 바뀌진않네 ..

직접 만들때는 내가 지정한 이름으로 ex)md0 로 만들어지지만
fstab 이나 그런걸로 reboot 하면 바로 자동으로 만들어지는건 md127 md126 으로 이름이 쓰여진다.

미니프로젝트 여도 개발문서를 만들거다. 
계획서 만들어서 할거고 , 모든 미니 프로젝트는 발표 ppt를 준비해야된다.
프레젠테이션을 한사람이 하면 다른사람이 질의응답

회의 내용을 정리해서 협업툴은 Notion 이다 . sample 을 보여주면 

pdf :  pptx, hwp 로하면안됨 pdf 로 변환해서 첨부해야 좋다 .그렇게 해

md raid1 로 두개 만들고 그 md 두개를 md 하는게 1 + 0 하는 방법

mirror volume 을 두개 만들고 striping volume 으로 만든것이다.
md는 meta physical volume 이 아니다 . 

미러링 되는 디스크 에는 쓸수가 있다.
mdadm 물리적인 하드가 있다고 생각하고 meta로 만든것이다. 메타 논리적 ? 하드웨어 적인 RAID를 소프트웨어 적으로 만들어준게 mdadm 이다.

lvm은 물리적인 디스크를 다루는 내용이다. 물리적으로 하드를 추가하고 쓸수있다.

# ll /dev/disk/by-uuid
# blkid | grep md

359c6756-97b6-492f-b3ac-2559c763017c md2 - uuid

공식적인 리눅스 평가는 요걸로한다? 캡쳐를 줘야된다

디스크 다끄고  1G 4개  => 2G 두개로 바꿀꺼다

fstab 에 새로운 uuid로 계속 쓸수있게 만들어줘야한다

lvm 은 유연하다. raid 미러블 하다.
lvm은 파티션을 조작하는거다 . 제공하는거다. 안되는거는 아니지만 partition type을 바꿔주고 해야되니까 

포인트 위에만 mount 되어있는거다 umount 하면 그대로 데이터 남아있다.

md 는 UUID 로 적어놔야지 안없어진다. reboot 하면 변경되서 이다.

제조사가 디스크가 같아야한다. 같은 회사에 브랜드까지 맞춰서 넣는경우가 많다. 같은 용량이기때문에 patitioning 이라는게 
RAID 는 메타디스크 전체를 다룬다.

일반적인 디스크는 lvm을 제일 추천한다. 훨씬 간단하면서 모든 장단점을 모두 가진게 있다.
4번쨰 방식이다. 절대 복잡하지 않다. 너무 간단해

파일 시스템 zfs 라는 썬솔라닉스 유닉스에서 만들어진 파일시스템이다.
xfs 라든지 최신 트렌드인 파일시스템이 여기에 영향을 받았다.

lost found가 없다 난 문제날일이 없다 자신감? 

lvm과 RAID0 와 짬뽕한것

work 라는 걸로 만들었더니 work라는 mount 포인트도 지멋대로생겨버림 

pool name 을 지정하는것이고
disk가 pool 에참여 하는것이다?

raidz 특징 항상느리지 않음 항상 빨라지지도 않음 

disk로 부터 만들어져서 조금 느리다?
파일들을 묶어서 zpool 을구성한다? 이게 뭔뜻이여? 파일들로 zpool을 만들었다는건가?

내보내기 zpool export  보이지 않는다. 어떻게 다시 되돌리냐 
zpool import -d /dev 이걸로 숨겨진거 찾기가능

zfs 10% 기능을 가지고있다.

du -s . summary 요약 만 보여준다.

 h 가 사람보기좋게

fsck 파일시스템 체크 명령어 clean 뜨면 복구할게 없는거고 pass? 뜨면 복구하고있는것

badblocks badblock 체킹하는것 legacy 한 블럭을 만들떄 그곳을 건너뛰고 만든다.

superblock 이 0 번인데 그걸 백업으로 복사해놓는다 망가진건 primary 0 이 망가졌다는 소리다.

bs 4096 1block 정도 된다.
관리 할때 지칭은 pool name으로한다. 조작을 이걸로한다는것

커널은 하나로 압축되있는 형태로 되어있다. 초기화를 하면서 RAM으로 올라간다. 거기서 스스로 압축을 푼다. > sbin_init or systemd
MBR? 
이미 부팅을했을때 부팅메세지 보는법 # dmesg 숫자로 보여주고   ,# more /var/log/boot.log 넘어가면 OK 로 보여준다. 이게 부팅장면과 똑같은 상태이다.

splash 인트로프로그램 : 시작할때뜨는 잠깐 뜨는걸 말함 뭐지 splash 가 이거면 이미지? 모양 ?

런레벨 0~ 6 + S 라는 단계 
2번이 네트워크가 안되는 다중사용자
3번 GUI 가없는 다중사용
						커지는거		꺼지는거
runlevel은 순서가 존재한다 . 한번에 실행되지 않는다.0 , 1 , 2, 3, 4, 5 -> 5, 4, 3, 2, 1,

ls /etc/initab
inet?
특정한 서비스를 제공하는 프로세스를 뭐라 불렀나> daemone
데몬 구동 하는방식
inetd
stend-alone 로 키는법 각자 따로 뛰운다 빠르다 

inetd 가 대신 떠있는다. 평상시에 혼자 독보이죠. 실제로 그 서비스가 필요하면 그걸 깨운다. 평상시에 inetd 만 보이고 나머지는 안보인다.
스냅숏 : 현재 상태를 저장하는 정보 
SELinux security linux
22 ssh
23 telnet
25 smtp
80 http
110 pop3
143 imap
port 얘기

system control -> systemctl
service ssh restart 이걸로 한다?

systemctl is-active cron 하면 active 인지 보여준다

who -r 런레벨 확인
runlevel 런레벨 확인하는것

runlevel 변경은 원격으로안된다
isolate 고립 

systemctl isolate runlevel5
런레벨 1번 이나 2번이면 네트워크가 끊어진다

RAM => DISK 로 옮겨줘야됨
sync 라는 명령어가 해주는데
shutdown에만 sync가 있다.

지금은 명령어들이 다 systemctl 실행파일을 가르키고있다. 그래서 레거시에선 달랐는데 지금은 비슷할거다.
shutdown -k 사용자 에게 경고 메세지만 보내주는것이다. 메세지기능
런레벨 init 으로도 바뀜 init[ level num ]

uts? 상시 전원 시스템

/var/log wtmp 파일 시스템에 종료 기록을 남긴다.
last로 종료기록 볼수있다. cat으로 보면 깨져보임 last로 봐야한다
last만